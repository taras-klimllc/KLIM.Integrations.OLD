<Project>
  <PropertyGroup>
    <TargetFramework>net8.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <!-- Uncomment for preview features -->
    <!-- <LangVersion>preview</LangVersion> -->
    <Deterministic>true</Deterministic>
  </PropertyGroup>
</Project>